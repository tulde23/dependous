namespace Dependous
{
    /// <summary>
    /// A marker interface indicating that you wish to register a concrete instance with the
    /// container as a singleton  dependency.
    /// </summary>
    public interface ISelfSingleton
    {
    }
}